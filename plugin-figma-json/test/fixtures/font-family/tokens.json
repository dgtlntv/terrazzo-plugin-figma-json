{
  "font": {
    "family": {
      "$type": "fontFamily",
      "primary": {
        "$value": "Inter",
        "$description": "Primary font family as string"
      },
      "secondary": {
        "$value": "Roboto",
        "$description": "Secondary font family as string"
      },
      "with-fallbacks": {
        "$value": ["Helvetica Neue", "Helvetica", "Arial", "sans-serif"],
        "$description": "Font with fallback stack (will be truncated)"
      },
      "code": {
        "$value": ["JetBrains Mono", "Fira Code", "monospace"],
        "$description": "Monospace font with fallbacks"
      },
      "single-array": {
        "$value": ["Georgia"],
        "$description": "Single font in array format"
      }
    }
  }
}
